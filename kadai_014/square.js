function functionName(parameters) {
  // 関数の処理
  return value;  // 戻り値
}

const functionName = (parameters) => {
  // 関数の処理
  return value;  // 戻り値
}

const functionName = (parameters) => value;  // 戻り値

